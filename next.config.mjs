/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ship source maps in production so stack traces resolve correctly in devtools.
  productionBrowserSourceMaps: true,
};

export default nextConfig;
